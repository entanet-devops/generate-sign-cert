{
  "signing": {
    "default": {
      "expiry": "{{ cert_expiry }}",
      "usages": [
        "signing",
        "key encipherment",
        "server auth",
        "client auth"
      ]
    }
  }
}